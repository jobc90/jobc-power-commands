interface:
  display_name: "Claude Dashboard"
  short_description: "Configure Claude Dashboard as the Claude Code statusline"
  default_prompt: "Use $claude-dashboard to update ~/.claude/settings.json and configure the dashboard statusline."
  policy:
    allow_implicit_invocation: true

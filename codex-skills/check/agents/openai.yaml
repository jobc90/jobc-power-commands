interface:
  display_name: "Check"
  short_description: "Review, fix, verify, and prep git handoff"
  default_prompt: "Use $check to review the current diff, apply safe fixes, verify them, and prepare the next git step."
policy:
  allow_implicit_invocation: true

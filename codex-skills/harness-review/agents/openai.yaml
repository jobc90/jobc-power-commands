interface:
  display_name: "Harness Review"
  short_description: "Run the review, fix, verify, and git handoff harness"
  default_prompt: "Use $harness-review to scan the diff, analyze issues, fix what is safe, verify the result, and prepare git handoff."
  policy:
    allow_implicit_invocation: true

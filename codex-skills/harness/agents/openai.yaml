interface:
  display_name: "Harness"
  short_description: "Run the Scout -> Planner -> Builder -> Refiner -> QA app-build harness"
  default_prompt: "Use $harness to run the full app-build harness with scale classification, planning, refinement, QA, and final reporting."
  policy:
    allow_implicit_invocation: true

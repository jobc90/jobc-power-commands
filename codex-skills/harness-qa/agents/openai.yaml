interface:
  display_name: "Harness QA"
  short_description: "Run scenario-based functional QA against a live app"
  default_prompt: "Use $harness-qa to scout the app, write scenarios, execute Playwright tests, analyze failures, and report QA results."
  policy:
    allow_implicit_invocation: true

interface:
  display_name: "Harness Docs"
  short_description: "Run the Researcher -> Outliner -> Writer -> Reviewer + Validator docs harness"
  default_prompt: "Use $harness-docs to research, outline, draft, review, validate, and finalize documentation through the harness workflow."
  policy:
    allow_implicit_invocation: true
